<div id="block_<%= block.id %>" class="wrapper links">
  <h1>Links to help you</h1>
  
  <ul id="block_<%= block.id %>">
    <% block.children.each do |child| %><%= block.render(child, local_assigns) %><% end %>
    <% if modal %><li>Click to add links.</li><% end %>
  </ul>
</div>

